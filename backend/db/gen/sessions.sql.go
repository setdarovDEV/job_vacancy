// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: sessions.sql

package gen

import (
	"context"
	"net/netip"
	"time"

	"github.com/google/uuid"
)

const changePasswordRevokeOthers = `-- name: ChangePasswordRevokeOthers :many
WITH pw AS (
    UPDATE users SET password_hash = $3 WHERE id = $1
)
UPDATE user_sessions s SET revoked_at = now()
WHERE s.user_id = $1 AND s.id <> $2 AND s.revoked_at IS NULL
RETURNING s.id
`

type ChangePasswordRevokeOthersParams struct {
	UserID       uuid.UUID
	KeepSession  uuid.UUID
	PasswordHash *string
}

// Set a new password and sign out every other session in one statement (TZ BE-12);
// returns the revoked session ids for the Redis revocation list.
func (q *Queries) ChangePasswordRevokeOthers(ctx context.Context, arg ChangePasswordRevokeOthersParams) ([]uuid.UUID, error) {
	rows, err := q.db.Query(ctx, changePasswordRevokeOthers, arg.UserID, arg.KeepSession, arg.PasswordHash)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []uuid.UUID{}
	for rows.Next() {
		var id uuid.UUID
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const createSession = `-- name: CreateSession :one
INSERT INTO user_sessions (user_id, refresh_hash, platform, user_agent, ip, expires_at)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, user_id, refresh_hash, prev_refresh_hash, platform, user_agent, ip, created_at, last_used_at, expires_at, revoked_at
`

type CreateSessionParams struct {
	UserID      uuid.UUID
	RefreshHash []byte
	Platform    string
	UserAgent   string
	Ip          *netip.Addr
	ExpiresAt   time.Time
}

func (q *Queries) CreateSession(ctx context.Context, arg CreateSessionParams) (UserSession, error) {
	row := q.db.QueryRow(ctx, createSession,
		arg.UserID,
		arg.RefreshHash,
		arg.Platform,
		arg.UserAgent,
		arg.Ip,
		arg.ExpiresAt,
	)
	var i UserSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RefreshHash,
		&i.PrevRefreshHash,
		&i.Platform,
		&i.UserAgent,
		&i.Ip,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.ExpiresAt,
		&i.RevokedAt,
	)
	return i, err
}

const deleteExpiredSessions = `-- name: DeleteExpiredSessions :execrows
DELETE FROM user_sessions
WHERE expires_at < now() - interval '7 days'
   OR revoked_at < now() - interval '7 days'
`

func (q *Queries) DeleteExpiredSessions(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, deleteExpiredSessions)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getSession = `-- name: GetSession :one
SELECT id, user_id, refresh_hash, prev_refresh_hash, platform, user_agent, ip, created_at, last_used_at, expires_at, revoked_at FROM user_sessions WHERE id = $1
`

func (q *Queries) GetSession(ctx context.Context, id uuid.UUID) (UserSession, error) {
	row := q.db.QueryRow(ctx, getSession, id)
	var i UserSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RefreshHash,
		&i.PrevRefreshHash,
		&i.Platform,
		&i.UserAgent,
		&i.Ip,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.ExpiresAt,
		&i.RevokedAt,
	)
	return i, err
}

const getSessionByPrevRefreshHash = `-- name: GetSessionByPrevRefreshHash :one
SELECT id, user_id, refresh_hash, prev_refresh_hash, platform, user_agent, ip, created_at, last_used_at, expires_at, revoked_at FROM user_sessions WHERE prev_refresh_hash = $1
`

func (q *Queries) GetSessionByPrevRefreshHash(ctx context.Context, prevRefreshHash []byte) (UserSession, error) {
	row := q.db.QueryRow(ctx, getSessionByPrevRefreshHash, prevRefreshHash)
	var i UserSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RefreshHash,
		&i.PrevRefreshHash,
		&i.Platform,
		&i.UserAgent,
		&i.Ip,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.ExpiresAt,
		&i.RevokedAt,
	)
	return i, err
}

const getSessionByRefreshHash = `-- name: GetSessionByRefreshHash :one
SELECT id, user_id, refresh_hash, prev_refresh_hash, platform, user_agent, ip, created_at, last_used_at, expires_at, revoked_at FROM user_sessions WHERE refresh_hash = $1
`

func (q *Queries) GetSessionByRefreshHash(ctx context.Context, refreshHash []byte) (UserSession, error) {
	row := q.db.QueryRow(ctx, getSessionByRefreshHash, refreshHash)
	var i UserSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.RefreshHash,
		&i.PrevRefreshHash,
		&i.Platform,
		&i.UserAgent,
		&i.Ip,
		&i.CreatedAt,
		&i.LastUsedAt,
		&i.ExpiresAt,
		&i.RevokedAt,
	)
	return i, err
}

const listActiveSessions = `-- name: ListActiveSessions :many
SELECT id, user_id, refresh_hash, prev_refresh_hash, platform, user_agent, ip, created_at, last_used_at, expires_at, revoked_at FROM user_sessions
WHERE user_id = $1 AND revoked_at IS NULL AND expires_at > now()
ORDER BY last_used_at DESC
`

func (q *Queries) ListActiveSessions(ctx context.Context, userID uuid.UUID) ([]UserSession, error) {
	rows, err := q.db.Query(ctx, listActiveSessions, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []UserSession{}
	for rows.Next() {
		var i UserSession
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.RefreshHash,
			&i.PrevRefreshHash,
			&i.Platform,
			&i.UserAgent,
			&i.Ip,
			&i.CreatedAt,
			&i.LastUsedAt,
			&i.ExpiresAt,
			&i.RevokedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeAllUserSessions = `-- name: RevokeAllUserSessions :many
UPDATE user_sessions SET revoked_at = now()
WHERE user_id = $1 AND revoked_at IS NULL
RETURNING id
`

func (q *Queries) RevokeAllUserSessions(ctx context.Context, userID uuid.UUID) ([]uuid.UUID, error) {
	rows, err := q.db.Query(ctx, revokeAllUserSessions, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []uuid.UUID{}
	for rows.Next() {
		var id uuid.UUID
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeSession = `-- name: RevokeSession :exec
UPDATE user_sessions SET revoked_at = now() WHERE id = $1 AND revoked_at IS NULL
`

func (q *Queries) RevokeSession(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, revokeSession, id)
	return err
}

const revokeUserSession = `-- name: RevokeUserSession :execrows
UPDATE user_sessions SET revoked_at = now()
WHERE id = $1 AND user_id = $2 AND revoked_at IS NULL
`

type RevokeUserSessionParams struct {
	ID     uuid.UUID
	UserID uuid.UUID
}

func (q *Queries) RevokeUserSession(ctx context.Context, arg RevokeUserSessionParams) (int64, error) {
	result, err := q.db.Exec(ctx, revokeUserSession, arg.ID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const rotateSession = `-- name: RotateSession :execrows
UPDATE user_sessions
SET prev_refresh_hash = refresh_hash,
    refresh_hash      = $1,
    last_used_at      = now(),
    expires_at        = $2,
    ip                = $3,
    user_agent        = $4
WHERE id = $5
  AND refresh_hash = $6
  AND revoked_at IS NULL
`

type RotateSessionParams struct {
	NewHash   []byte
	ExpiresAt time.Time
	Ip        *netip.Addr
	UserAgent string
	ID        uuid.UUID
	OldHash   []byte
}

// Rotation is conditional on the old hash so two concurrent refreshes can't both win.
func (q *Queries) RotateSession(ctx context.Context, arg RotateSessionParams) (int64, error) {
	result, err := q.db.Exec(ctx, rotateSession,
		arg.NewHash,
		arg.ExpiresAt,
		arg.Ip,
		arg.UserAgent,
		arg.ID,
		arg.OldHash,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
